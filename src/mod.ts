/**
 * Makes a sum of 2 numbers
 * @param a The 1st number to sum
 * @param b The 2nd number to sum
 * @returns A sum of the 2 numbers
 */
export function add(a: number, b: number): number {
  return a + b;
}
